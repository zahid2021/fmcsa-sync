# Client video — 2026-08-24 (`bandicam 2026-08-24 16-36-26-199.mp4`)

**IMPORTANT:** Filter **TN** + **Total Items: 79621** is only a **live EXAMPLE** the client showed.
It is **NOT** the deliverable (“fill TN” / “keep 8 chunks”).

## Audio (what client actually asks)

1. **Backup first** — full project backup **before** any change  
2. **DB backup** — from Anthony machine (Workbench / mysqldump) and **send on Slack + email**  
3. **Pagination** — page should load **~1000 rows**; **Total Items** must show the **real full count** (in demo that was ~79621)  
4. **Export** — **remove Chunks**; download **all filtered data in ONE file** (chunks exist only because Excel times out)

## What the screen shows (demo only)

- RDP → anthoney → `localhost:5173` FMCSA CARRIERS  
- Example filter: value `TN`, column `PHY_ST` → Total Items ~79621  
- Opens Chunks 1–8 + EXPORT TO EXCEL ← client wants this **gone** (one file instead)

## Correct deliverables (priority)

| # | Ask | How |
|---|-----|-----|
| 1 | Project + DB backup → Slack/email | `python backup_for_client.py` |
| 2 | UI: Total Items = full count, ~1000 rows/page | Wire Vite UI → `api_server.py` `/api/carriers?limit=1000` |
| 3 | No chunks; one-file export | `export_one_file.py` or `/api/carriers/export` |

## Do NOT treat as the main task

- Filling “missing ~20 TN rows”  
- Building/keeping 8 Excel chunks  
- Re-importing full USA census (already done earlier in project history)

TN export was only a **script test**, not the client goal.
